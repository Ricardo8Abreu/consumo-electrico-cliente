/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,

  // images: {
  //   // loader: 'custom',
  //   domains: ["localhost", process.env.API_DOMINIO]
  // }
}

module.exports = nextConfig
